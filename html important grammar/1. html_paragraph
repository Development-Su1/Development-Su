단락(paragraph)
단락의 여백 크기는 CSS를 통해서 설정이 가능합니다.

- HTML에서는 <P>테그를 사용하여 단락을 표현합니다.
1) 예제
<html>

  <head>
    <meta charset="utf-8">
    <title>HTML Paragraph</title>
  </head>

  <body>
<p>HyperText Markup Language, commonly referred to as HTML, is the standard markup language uesd to create web pages.</p>

<p>HTML elements form the building blocks of all websites.</p>

<p>The language is written in the form of HTML elements consisting of tags enclosed in angle brackets.</p>
  
  </body>

</html>

- <br>태그를 사용하면 새로운 단락을 만들지 않고도 줄을 나눌 수 있습니다.
- 그러나 의미적으로 단락은 <p>태그를 사용해야 됩니다.
2) 예제
<p>
HyperText Markup Language<br>
commonly referred to as HTML<br>
<br>
</p>

- <p>태그와 <br>태그의 차이
<p>태그는 단락별로 열림과 닫힌태그를 입력해야 합니다. 그러나 <br>태그는 열린태그만 입력하면 됩니다.
<br>태그는 한번만 입력하면 한줄만 되고 두개를 입력하면 두줄, 세개를 입력하면 세줄이 됩니다.
<p>태그는 하나의 단락이라는 정보가 표현되지만 <br>태그는 시각적으로 줄바꿈을 표시한 것입니다.
따라서 하나의 단락을 표현하고 싶으면 <p>태그를 사용하며 CSS로 간격을 조절하는 것이 가장 좋습니다.
